package PCRD::Config::Memory;

use v5.14;
use warnings;

use PCRD::Mite;

extends 'PCRD::Config';

sub load_config
{
	my ($self) = @_;

	# nothing to do here - 'values' should be passed to constructor
}

1;

